<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>dev.bwmp</groupId>
    <artifactId>holopanels-parent</artifactId>
    <version>2.0.0</version> <!-- x-release-please-version -->
    <packaging>pom</packaging>

    <name>HoloPanels</name>
    <description>Interactive in-world panel framework for Paper, Purpur, Pufferfish and Folia servers.</description>

    <licenses>
        <license>
            <name>GNU Lesser General Public License v3.0</name>
            <url>https://www.gnu.org/licenses/lgpl-3.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <modules>
        <module>holopanels-api</module>
        <module>holopanels-plugin</module>
    </modules>

    <distributionManagement>
        <repository>
            <id>nexus-site</id>
            <name>maven-releases</name>
            <url>https://nexus.bwmp.dev/repository/maven-releases/</url>
        </repository>
        <snapshotRepository>
            <id>nexus-site</id>
            <name>maven-snapshots</name>
            <url>https://nexus.bwmp.dev/repository/maven-snapshots/</url>
        </snapshotRepository>
    </distributionManagement>

    <properties>
        <java.version>17</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <paper.version>1.19.4-R0.1-SNAPSHOT</paper.version>
        <packetevents.version>2.13.0</packetevents.version>
        <placeholderapi.version>2.12.2</placeholderapi.version>
        <junit.version>5.13.4</junit.version>
    </properties>

    <repositories>
        <!--
            Keystone lives here. This does NOT help Maven find keystone-parent
            though: a parent is resolved before the child's repositories are
            read, so that comes from settings.xml or a local install. See the
            Jenkinsfile and docs/jenkins.md in Keystone.
        -->
        <repository>
            <id>bwmp-nexus</id>
            <url>https://nexus.bwmp.dev/repository/maven-public/</url>
        </repository>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <repository>
            <id>codemc-releases</id>
            <url>https://repo.codemc.io/repository/maven-releases/</url>
        </repository>
        <repository>
            <id>placeholderapi</id>
            <url>https://repo.extendedclip.com/content/repositories/placeholderapi/</url>
        </repository>
    </repositories>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.papermc.paper</groupId>
                <artifactId>paper-api</artifactId>
                <version>${paper.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.retrooper</groupId>
                <artifactId>packetevents-spigot</artifactId>
                <version>${packetevents.version}</version>
            </dependency>
            <dependency>
                <groupId>me.clip</groupId>
                <artifactId>placeholderapi</artifactId>
                <version>${placeholderapi.version}</version>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${junit.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.14.0</version>
                    <configuration>
                        <release>${java.version}</release>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.5.3</version>
                    <configuration>
                        <useModulePath>false</useModulePath>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
